# Habit Flywheel vNext Todo

> 基于 `docs/plan.md` 拆解的可执行实施清单。
> 使用方式：按阶段推进，优先完成前置阶段再进入后续阶段；每项完成后勾选。

## 执行规则

- [x] 始终以 `docs/plan.md` 作为方向依据，不再按旧版模块式文档推进。
- [x] 采用渐进式迁移，不做推倒重来式重写。
- [x] 保留现有 `Welcome / Auth / App` 入口结构。
- [x] 保留现有认证、PWA、i18n、深浅色主题、响应式基础设施。
- [x] 保留现有打卡、取消打卡、乐观更新、失败回滚的交互语义。

## Phase 0：基线整理

目标：先把当前系统的边界、复用件和迁移顺序摸清楚，避免重构时反复返工。

### 0.1 当前实现盘点

- [x] 盘点当前认证后应用壳层的页面结构、导航状态和模块切换逻辑。
- [x] 盘点可复用 hooks：`useHabits`、`useRewards`、`useHabitCompletions`、`useAuth`、`useSettings`。
- [x] 盘点可复用组件：卡片、表单、对话框、PWA 提示、用户账户、语言切换等。
- [x] 标记当前只存在 UI 外壳、未真正实现的功能点。

### 0.2 技术基线

- [x] 记录当前 `build` 状态和 bundle 警告。
- [x] 记录当前 `lint` 错误清单，按类型分组。
- [x] 确认当前数据库表结构、RLS、迁移版本和前端类型定义一致。
- [x] 列出所有仍未国际化的页面和文案位置。

### 0.3 迁移策略

- [x] 定义“旧数据如何兼容 vNext”的迁移原则。
- [x] 定义“旧导航如何平滑迁移到新导航”的切换方案。
- [x] 明确哪些旧页面先保留为兼容页面，哪些页面会被直接替换。

## Phase 1：应用骨架重构

目标：先把认证后应用从“模块面板”改成“reward-first shell”。

### 1.1 新应用壳层

- [x] 重构认证后应用的一级导航为 `Dashboard / Plans / Review / Library / Settings`。
- [x] 保持 `/`、`/auth`、`/app` 入口不变，仅调整 `/app` 内部结构。
- [x] 将当前通过本地状态切换模块的方式，升级为更清晰的视图结构或子路由结构。
- [x] 让移动端和桌面端同时支持新的导航层级。

### 1.2 页面占位与迁移壳

- [x] 新建 `Dashboard` 页面骨架。
- [x] 新建 `Plans` 页面骨架。
- [x] 新建 `Review` 页面骨架。
- [x] 新建 `Library` 页面骨架。
- [x] 保留 `Settings` 页面并迁移到新壳层。

### 1.3 旧模块收编

- [x] 移除 `Bindings` 一级导航入口。
- [x] 将 `Habits` 与 `Rewards` 从一级导航降级为计划上下文中的子能力。
- [x] 保留旧模块代码可访问，作为过渡期参考或兼容实现。
- [x] 为过渡期页面标注清晰的迁移入口，避免功能丢失。

### 1.4 Phase 1 完成标准

- [x] 用户登录后默认进入新的飞轮控制台，而不是旧版模块面板。
- [x] 新旧入口不冲突，`Welcome / Auth / App` 整体路由可正常访问。
- [x] 桌面端和移动端都能完成主导航切换。

## Phase 2：领域模型与数据库升级

目标：让数据结构真正支持 reward-first 产品模型。

### 2.1 前端领域模型

- [x] 新增 `RewardPlan` 类型定义，而不是继续把奖励仅视为简单兑换项。
- [x] 为计划状态定义枚举：`wish / validating / active / ready_to_redeem / redeemed / abandoned`。
- [x] 为奖励类型定义枚举：`one_time / repeatable`。
- [x] 为习惯补齐 `frequency` 与 `target_count` 的前端类型定义。
- [x] 为完成记录补齐 `note`、`plan_id_snapshot`、`frequency_bucket` 等字段定义。

### 2.2 数据库迁移

- [x] 编写 Supabase migration，为奖励补齐 `type`、`status`、`target_date`、`motivation_note`。
- [x] 编写 Supabase migration，为习惯补齐 `frequency`、`target_count`。
- [x] 设计并创建显式绑定关系表，避免长期依赖单外键模式。
- [x] 为完成记录补齐备注、计划快照和频率桶字段。
- [x] 评估是否需要为历史数据做默认值回填和状态初始化。

### 2.3 类型与兼容

- [x] 更新 Supabase 生成类型或手写类型封装，确保前后端字段对齐。
- [x] 为旧版 reward/habit 数据写兼容映射逻辑。
- [x] 确保已有用户不因新字段缺失而出现运行时错误。

### 2.4 Phase 2 完成标准

- [x] 新老数据都能被前端正确读取。
- [x] 数据结构已支持计划状态、习惯频率目标和显式绑定关系。
- [x] 前端不再把频率和目标次数仅停留在表单占位层。

## Phase 3：计划中心能力

目标：把奖励计划变成真正的一等对象。

### 3.1 Plans 列表页

- [x] 实现按状态分组的计划列表视图：`Active / Validating / Ready / Redeemed / Abandoned`。
- [x] 为计划卡片展示名称、状态、当前能量、所需能量、绑定习惯数、最近推进时间。
- [x] 支持移动端和桌面端的一致浏览体验。
- [x] 提供空状态和首次引导。

### 3.2 Plan Detail 页面

- [x] 新建计划详情页，作为绑定、推进、兑现、放弃的主操作页。
- [x] 展示计划概览、动机说明、进度信息和近期记录。
- [x] 在详情页中完成绑定管理，不再保留独立 `Bindings` 页面。
- [x] 支持计划状态切换：进入验证、激活、标记可兑现、兑现、放弃。

### 3.3 绑定能力收编

- [x] 将现有绑定逻辑从“独立页面能力”改为“计划详情中的子模块”。
- [x] 支持把一个或多个习惯绑定到同一个计划。
- [x] 明确过渡期是否只支持 `exclusive` 绑定模式。
- [x] 处理旧版“习惯直接绑定奖励”的数据兼容显示。

### 3.4 Phase 3 完成标准

- [x] 用户可以在 `Plans` 中管理主要奖励计划，而不需要来回切旧版模块页。
- [x] 绑定创建、解除、查看都在计划详情中闭环完成。

## Phase 4：从奖励开始的创建流程

目标：让新建流程真正符合产品理念。

### 4.1 创建入口重构

- [x] 将当前“先建习惯/先建奖励”的独立入口调整为“先建计划”的主入口。
- [x] 在 `Dashboard` 和 `Plans` 中提供一致的新建计划入口。
- [x] 保留过渡期的旧表单入口，但降低其主导航权重。

### 4.2 新建计划向导

- [x] 设计并实现多步创建流程。
- [x] 第一步：填写奖励名称与描述。
- [x] 第二步：选择奖励类型（一次性 / 可重复）。
- [x] 第三步：填写动机说明与目标条件。
- [x] 第四步：配置习惯与每次推进量。
- [x] 第五步：确认计划状态与初始化信息。

### 4.3 计划内习惯配置

- [x] 在向导中支持新建习惯并直接绑定到当前计划。
- [x] 支持从已有习惯库中选择并绑定到当前计划。
- [x] 支持设置 `daily / weekly / monthly` 和 `target_count`。
- [x] 支持设置每次完成产生的推进值或能量值。

### 4.4 Phase 4 完成标准

- [x] 新用户可以不经过旧版模块页，直接完成“创建奖励计划 -> 绑定习惯 -> 开始行动”。
- [x] 新建流程默认从奖励出发，而不是从习惯出发。

## Phase 5：Dashboard 重构

目标：让首页回答“今天最值得做什么”。

### 5.1 Dashboard 主模块

- [x] 实现 Hero 区，展示当前主计划和剩余差距。
- [x] 实现 Today Actions 区，按影响最大排序显示今日应做习惯。
- [x] 实现 Momentum Strip，展示连续行动、本周推进和累计全局能量。
- [x] 实现 Validation Signals，展示最近完成、最近放弃和最近重点投入领域。

### 5.2 今日行动逻辑

- [x] 设计“今日应做”排序规则，而不是按创建时间硬排。
- [x] 根据习惯频率和目标周期计算今天需要关注的行动。
- [x] 明确当用户没有 active 计划时的空状态和引导。

### 5.3 Phase 5 完成标准

- [x] 用户进入应用后 5 秒内能理解当前主目标。
- [x] 首页不再依赖旧版模块式网格来完成主操作。

## Phase 6：打卡、推进与反馈重构

目标：把每次完成都明确映射到计划推进。

### 6.1 打卡逻辑重构

- [x] 更新打卡逻辑，使完成记录明确关联计划上下文。
- [x] 保留现有乐观更新、取消打卡和回滚机制。
- [x] 根据频率规则处理每日/每周/每月目标的完成统计。

### 6.2 即时反馈

- [x] 打卡后显示“本次推进了哪个计划”。
- [x] 打卡后显示“推进了多少、距离兑现还差多少”。
- [x] 达到关键阈值时触发更强的视觉和文案反馈。
- [x] 取消打卡时同步回退计划推进与奖励能量。

### 6.3 兑现与放弃流程

- [x] 设计并实现奖励兑现确认层。
- [x] 在兑现时展示“由哪些习惯换来、用了多久、完成了多少次行动”。
- [x] 设计并实现“放弃计划”流程。
- [x] 记录放弃原因、已投入行动和学习收获。

### 6.4 Phase 6 完成标准

- [x] 打卡反馈不再只是抽象能量变化，而是清晰的计划推进反馈。
- [x] 兑现和放弃都成为有记录、有意义的产品事件。

## Phase 7：Review 重构

目标：让复盘页帮助用户认识自己，而不是只看统计数字。

### 7.1 Review 页面结构

- [x] 将旧版 `History` 页面升级为 `Review`。
- [x] 设计周/月维度切换。
- [x] 设计计划视角和习惯视角的复盘切换。

### 7.2 洞察模块

- [x] 实现能量趋势图。
- [x] 实现奖励推进排行。
- [x] 实现习惯贡献排行。
- [x] 实现“已验证成功的奖励”模块。
- [x] 实现“被过滤掉的欲望”模块。
- [x] 评估是否加入行动密度热力图。
  当前版本暂不加入。现有 `Review` 已有趋势图、排行和验证结果摘要，热力图会增加复杂度但不会明显提升当前主线判断效率。

### 7.3 Phase 7 完成标准

- [x] Review 能回答“我最近在为谁投入、哪些欲望值得继续、哪些该放弃”。
- [x] 旧版纯统计历史页可以退居兼容层或被正式替换。

## Phase 8：Library 与归档体系

目标：为长期使用提供沉淀区，而不是让首页和计划页越来越乱。

### 8.1 Library 页面

- [x] 新建 `Library` 页面。
- [x] 收纳归档习惯、已完成计划、已放弃计划和未来模板。
- [x] 明确 `Library` 与 `Plans` 的边界。

### 8.2 模板与复用

- [x] 设计通用习惯模板结构。
- [x] 设计可重复奖励的复用机制。
- [x] 评估是否加入推荐模板和计划复制能力。

### 8.3 Phase 8 完成标准

- [x] 历史沉淀内容不再挤占当前执行页。
- [x] 未来的模板化和复用能力有明确落点。

## Phase 9：Settings 补完

目标：把当前“有 UI 无落地”的设置项补成可用能力。

### 9.1 数据管理

- [x] 实现导出数据。
- [x] 实现导入数据。
- [x] 实现重置默认或明确废弃该入口。
- [x] 实现清空数据或明确废弃该入口。

### 9.2 设置页整合

- [x] 检查设置页文案是否全部国际化。
- [x] 清理无效说明、过时版本号和过时更新时间。
- [x] 为实验功能和未来开关预留区域。

### 9.3 Phase 9 完成标准

- [x] 设置页中的操作按钮都真实可用，或被明确移除。

## Phase 10：工程收口

目标：把 vNext 从“能跑”收口到“可持续维护”。

### 10.1 国际化

- [x] 补齐 `Index`、`History/Review`、`Binding` 迁移后页面中的硬编码文案。
- [x] 补齐 `Auth`、`Settings`、空状态、提示文案的中英日翻译。
- [x] 检查新增计划状态、奖励类型和复盘模块的翻译键。

### 10.2 类型与 lint

- [x] 清理核心业务流中的 `any`。
- [x] 修复 `react-hooks/exhaustive-deps` 警告。
- [x] 修复无效 ESLint 规则和生成产物被 lint 扫描的问题。
- [x] 统一领域类型定义，减少页面内临时映射。

### 10.3 性能与拆包

- [x] 为 `Dashboard`、`Plans`、`Review` 做按路由或按模块拆包。
- [x] 控制主包体积，消除大 chunk 警告。
- [x] 评估图表、PWA、历史模块的懒加载策略。

### 10.4 测试与验证

- [x] 增加关键用户流测试：登录、创建计划、绑定习惯、打卡、取消打卡、兑现、放弃。
- [x] 增加数据库迁移后的回归验证。
- [x] 增加移动端和桌面端的关键交互检查。
- [x] 验证 `build`、`lint`、核心流程运行状态全部通过。

### 10.5 Phase 10 完成标准

- [x] `lint` 通过。
- [x] `build` 通过。
- [x] 关键用户流稳定可回归。
- [x] 当前有效文档与实际实现保持一致。

## Phase 11：视觉系统重构

目标：让 `Welcome / Auth / App / Dialogs` 共享统一气质，并让状态与关键操作拥有明确颜色语义。

### 11.1 视觉基线

- [x] 以 [visual-system.md](./visual-system.md) 作为当前配色与视觉重构依据。
- [x] 在 `src/index.css` 中清理旧版 `紫 + 金` 主导 token。
- [x] 建立新的 light / dark 主题 token：纸白、墨绿、铜色、暖灰、铁锈红。
- [x] 将计划状态色从通用主色中拆出，建立 `wish / validating / active / ready_to_redeem / redeemed / abandoned` 的独立语义色。

### 11.2 认证后应用壳层

- [x] 重构 `Index` 壳层中的侧边栏、顶部栏、Hero、里程碑卡的配色。
- [x] 去掉大面积紫金渐变，改为中性背景 + 深青绿主色 + 铜色强调。
- [x] 让 `Dashboard / Plans / Review / Library / Settings` 形成清晰层级，而不是同一套彩色卡片平铺。

### 11.3 弹窗层

- [x] 为 `PlanComposer` 设计“构建 / 开始一轮验证”语义的弹窗配色。
- [x] 为 `PlanDetailDialog` 设计更克制的运营面板式弹窗配色。
- [x] 为 `RedeemPlanDialog` 设计带仪式感但不过度高饱和的兑现弹窗配色。
- [x] 为 `AbandonPlanDialog` 设计偏反思语义的灰红 / 铁锈红弹窗配色。
- [x] 为 `LegacyWorkspaceDialog` 设计去饱和兼容层配色，明确它不是主舞台。
- [x] 统一弹窗遮罩、边框、标题区、主按钮、次按钮和危险按钮的层级规则。

### 11.4 欢迎页

- [x] 将欢迎页从“紫色产品落地页”改为“编辑感宣言页”。
- [x] 重做 Hero 背景、预览卡片、功能卡片和 CTA 的配色体系。
- [x] 让欢迎页与应用内共享同一气质，但保留更强的品牌叙事感。

### 11.5 完成标准

- [x] 欢迎页和应用内不再依赖紫金渐变作为默认视觉语言。
- [x] 页面层、卡片层、弹窗层形成稳定分层。
- [x] 用户可以一眼区分 `验证中 / 进行中 / 可兑现 / 已兑现 / 已放弃`。
- [x] 兑现、放弃、创建计划三类弹窗在气质上明显不同，但仍属于同一产品系统。

## 后续增强项

这些不是当前主线，但可在核心版本稳定后继续推进。

- [ ] 实现成就系统。
- [ ] 支持证据上传或外部链接记录。
- [ ] 支持通知能力和更完整的 PWA 体验增强。
- [ ] 支持模板库与推荐计划。
- [ ] 支持更丰富的可重复奖励策略。

## 建议执行顺序

- [x] 先完成 Phase 0 到 Phase 2，再开始大规模页面改造。
- [x] Phase 3 到 Phase 6 是 vNext 的产品核心，应连续推进。
- [x] Phase 7 到 Phase 9 在主流程稳定后进入。
- [x] Phase 10 在每个阶段中穿插处理，但以最终收口为准。
