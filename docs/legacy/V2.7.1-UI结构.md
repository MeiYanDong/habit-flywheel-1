> 历史参考文档，不代表当前主信息架构。
> 这份文档描述的是 v2.7.1 阶段的布局结构，保留用于回顾旧版页面组织方式。
> 当前产品结构请查看 `../plan.md`。

基于当时代码结构整理的旧版布局结构文档：

习惯飞轮 - 完整布局结构文档
📱 整体布局架构
布局层次结构
App.tsx (应用根组件)
├── QueryClientProvider (数据状态管理)
├── SettingsProvider (设置状态管理)
├── TooltipProvider (工具提示提供者)
├── Toaster (通知组件)
├── Sonner (增强通知)
└── BrowserRouter (路由管理)
    └── Routes
        ├── Route "/" → Index.tsx (主页面)
        └── Route "*" → NotFound.tsx (404页面)
🎨 主页面布局 (Index.tsx)
页面整体结构
┌─────────────────────────────────────────────────────────┐
│                    整体容器                               │
│  min-h-screen bg-gray-50 dark:bg-gray-900 flex        │
│                                                         │
│  ┌─────────────┐  ┌───────────────────────────────────┐ │
│  │    侧边栏    │  │          主内容区                  │ │
│  │   w-64      │  │         flex-1                    │ │
│  │             │  │        p-6 overflow-y-auto        │ │
│  │             │  │                                   │ │
│  │             │  │                                   │ │
│  │             │  │                                   │ │
│  │             │  │                                   │ │
│  │             │  │                                   │ │
│  │             │  │                                   │ │
│  └─────────────┘  └───────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
侧边栏详细结构 (264px 固定宽度)
┌─────────────────────────────┐
│          侧边栏头部          │
│         p-6                │
│  ┌─────────────────────────┐ │
│  │      应用标识区域        │ │
│  │    text-center mb-8    │ │
│  │  🌟 (emoji 图标)        │ │
│  │   "习惯飞轮" 标题        │ │
│  │    副标题描述           │ │
│  └─────────────────────────┘ │
│                            │
│          导航菜单区          │
│        space-y-2           │
│  ┌─────────────────────────┐ │
│  │  今日习惯              │ │
│  │  习惯管理              │ │
│  │  奖励管理              │ │
│  │  绑定管理              │ │
│  │  历史记录              │ │
│  │  设置中心              │ │
│  └─────────────────────────┘ │
└─────────────────────────────┘
主内容区布局 (flex-1 自适应宽度)
┌─────────────────────────────────────┐
│            主内容区                  │
│           flex-1 p-6               │
│                                    │
│  ┌─────────────────────────────────┐ │
│  │          页面标题区              │ │
│  │     text-center mb-6           │ │
│  │    h2 + 描述文字                │ │
│  └─────────────────────────────────┘ │
│                                    │
│  ┌─────────────────────────────────┐ │
│  │         统计卡片区               │ │
│  │  (条件渲染 - showStats时显示)   │ │
│  │    gradient bg card            │ │
│  └─────────────────────────────────┘ │
│                                    │
│  ┌─────────────────────────────────┐ │
│  │        内容网格区域              │ │
│  │  grid grid-cols-1 md:2 lg:3    │ │
│  │         gap-4                  │ │
│  │                                │ │
│  │  ┌─────┐ ┌─────┐ ┌─────┐        │ │
│  │  │卡片1│ │卡片2│ │卡片3│        │ │
│  │  └─────┘ └─────┘ └─────┘        │ │
│  │  ┌─────┐ ┌─────┐ ┌─────┐        │ │
│  │  │卡片4│ │卡片5│ │卡片6│        │ │
│  │  └─────┘ └─────┘ └─────┘        │ │
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘
🎯 各模块具体布局
1. 今日习惯模块布局
┌─────────────────────────────────────┐
│          今日习惯模块                │
│         space-y-6                  │
│                                    │
│  ┌─────────────────────────────────┐ │
│  │         页面标题                │ │
│  │      text-center               │ │
│  └─────────────────────────────────┘ │
│                                    │
│  ┌─────────────────────────────────┐ │
│  │      今日统计卡片               │ │
│  │   紫色渐变背景 + 能量显示        │ │
│  └─────────────────────────────────┘ │
│                                    │
│  ┌─────────────────────────────────┐ │
│  │       习惯卡片网格              │ │
│  │  grid-cols-1 md:2 lg:3         │ │
│  │                                │ │
│  │  [习惯卡片] [习惯卡片] [习惯卡片] │ │
│  │  [习惯卡片] [习惯卡片] [习惯卡片] │ │
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘
2. 习惯管理模块布局
┌─────────────────────────────────────┐
│         习惯管理模块                 │
│         space-y-6                  │
│                                    │
│  ┌─────────────────────────────────┐ │
│  │    顶部操作栏 (flex justify)     │ │
│  │  标题 + 描述     筛选器 + 按钮   │ │
│  └─────────────────────────────────┘ │
│                                    │
│  ┌─────────────────────────────────┐ │
│  │       习惯列表区域              │ │
│  │                                │ │
│  │  ┌─ 活跃习惯 (n) ─────────────┐ │ │
│  │  │                           │ │ │
│  │  │  [习惯卡片] [习惯卡片]     │ │ │
│  │  │  [习惯卡片] [习惯卡片]     │ │ │
│  │  │                           │ │ │
│  │  └───────────────────────────┘ │ │
│  └─────────────────────────────────┘ │
│                                    │
│  ┌─────────────────────────────────┐ │
│  │       表单对话框区域             │ │
│  │    (条件渲染 - 模态框)          │ │
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘
3. 奖励管理模块布局
┌─────────────────────────────────────┐
│         奖励管理模块                 │
│         space-y-6                  │
│                                    │
│  ┌─────────────────────────────────┐ │
│  │    顶部操作栏 (flex justify)     │ │
│  │  标题 + 描述     筛选器 + 按钮   │ │
│  └─────────────────────────────────┘ │
│                                    │
│  ┌─────────────────────────────────┐ │
│  │       奖励列表区域              │ │
│  │                                │ │
│  │  ┌─ 可兑换奖励 (n) ───────────┐ │ │
│  │  │                           │ │ │
│  │  │  [奖励卡片] [奖励卡片]     │ │ │
│  │  │  - 进度条显示             │ │ │
│  │  │  - 能量值显示             │ │ │
│  │  │  - 兑换按钮               │ │ │
│  │  │                           │ │ │
│  │  └───────────────────────────┘ │ │
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘
🎨 卡片组件布局标准
习惯卡片布局
┌─────────────────────────────┐
│         习惯卡片             │
│    Card + CardContent       │
│         p-4                │
│                            │
│  ┌─────────────────────────┐ │
│  │      卡片头部            │ │
│  │  标题 + 状态徽章         │ │
│  │  描述文字               │ │
│  └─────────────────────────┘ │
│                            │
│  ┌─────────────────────────┐ │
│  │     能量信息区           │ │
│  │  ⚡ +能量值             │ │
│  │  频率显示               │ │
│  │  完成进度               │ │
│  └─────────────────────────┘ │
│                            │
│  ┌─────────────────────────┐ │
│  │     操作按钮区           │ │
│  │  [编辑] [删除] [归档]    │ │
│  └─────────────────────────┘ │
│                            │
│  ┌─────────────────────────┐ │
│  │    绑定奖励信息          │ │
│  │  (条件显示)             │ │
│  └─────────────────────────┘ │
└─────────────────────────────┘
奖励卡片布局
┌─────────────────────────────┐
│         奖励卡片             │
│    Card + CardContent       │
│         p-4                │
│                            │
│  ┌─────────────────────────┐ │
│  │      卡片头部            │ │
│  │  标题 + 状态徽章         │ │
│  │  描述文字               │ │
│  └─────────────────────────┘ │
│                            │
│  ┌─────────────────────────┐ │
│  │     进度信息区           │ │
│  │  进度百分比             │ │
│  │  能量进度条             │ │
│  │  当前/目标能量值         │ │
│  └─────────────────────────┘ │
│                            │
│  ┌─────────────────────────┐ │
│  │     操作按钮区           │ │
│  │  [兑换]/[已兑换]        │ │
│  │  [编辑] [删除]          │ │
│  └─────────────────────────┘ │
└─────────────────────────────┘
📱 响应式断点设计
断点配置

/* 移动端 */
@media (max-width: 767px) {
  - 侧边栏: 隐藏或叠加显示
  - 网格: grid-cols-1 
  - 边距: p-4
}

/* 平板端 */
@media (768px - 1023px) {
  - 侧边栏: 可折叠
  - 网格: grid-cols-2
  - 边距: p-5
}

/* 桌面端 */
@media (1024px+) {
  - 侧边栏: 固定显示
  - 网格: grid-cols-3
  - 边距: p-6
}
响应式网格系统
超小屏 (<640px):  grid-cols-1
小屏   (640px+):  sm:grid-cols-1  
中屏   (768px+):  md:grid-cols-2
大屏   (1024px+): lg:grid-cols-3
超大屏 (1280px+): xl:grid-cols-4
🎯 深色模式布局
主题切换系统

/* 浅色模式 */
.light {
  background: bg-gray-50
  text: text-gray-900
  cards: bg-white border-gray-200
}

/* 深色模式 */
.dark {
  background: dark:bg-gray-900
  text: dark:text-gray-100  
  cards: dark:bg-gray-800 dark:border-gray-700
}
🔧 组件状态设计
加载状态
骨架屏占位
加载动画
禁用状态
空状态
空数据图标
引导文案
操作建议
错误状态
错误图标
错误信息
重试按钮
📊 数据流布局
状态管理层次
App Level:
├── SettingsContext (全局设置)
├── QueryClient (数据缓存)
└── Router State (路由状态)

Page Level:
├── Local State (页面状态)
├── Form State (表单状态)  
└── UI State (界面状态)

Component Level:
├── Props (属性传递)
├── Callbacks (事件回调)
└── Local State (组件状态)
这份布局结构文档涵盖了整个应用的布局层次、响应式设计、组件结构和状态管理，为开发和维护提供了完整的参考标准。
